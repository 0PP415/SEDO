<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F9FA"> <com.google.android.material.appbar.MaterialToolbar
    android:id="@+id/toolbar_preview"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@android:color/transparent"
    app:title="사진 확인"
    app:titleTextColor="#212121" app:navigationIcon="@android:drawable/ic_menu_revert"
    app:navigationIconTint="#212121" app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_preview_full"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:scaleType="fitCenter"
        app:layout_constraintTop_toBottomOf="@id/toolbar_preview"
        app:layout_constraintBottom_toTopOf="@id/layout_bottom_buttons" />

    <LinearLayout
        android:id="@+id/layout_bottom_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingVertical="24dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_retake"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="59dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:text="다시 선택"
            android:textColor="#1976D2"
            app:strokeColor="#1976D2" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_analyze"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="분석 시작하기"
            app:backgroundTint="#1976D2" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>